name: Wrong Workflow Example

on:
  workflow_dispatch:

jobs:
  terraform-plan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (WRONG - always main)
        uses: actions/checkout@v4

      - name: Show branch
        run: |
          echo "Current branch:"
          git branch --show-current
